---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: ruby
    tag: '2.1'

inputs:
  - name: source

outputs:
  - name: site
    path: site

run:
  path: sh
  args:
  - -exc
  - |
    cd source
    bundler install
    jekyll build --destination ../site